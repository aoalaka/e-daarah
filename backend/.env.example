# Server Configuration
PORT=5000
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
# Option 1: Railway (uses DATABASE_URL automatically)
# DATABASE_URL is automatically set by Railway when you add MySQL
# DATABASE_URL=mysql://user:password@host:port/database

# Option 2: Docker Compose / Local (uses individual vars)
DB_HOST=mysql
DB_PORT=3306
DB_USER=madrasah_user
DB_PASSWORD=madrasah_password
DB_NAME=madrasah_admin

# Optional: Enable SSL for Railway MySQL
# DB_SSL=true

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:3000

# Email Configuration (for password reset emails)
# For production, use real SMTP credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@madrasah.com

# For development testing with Ethereal Email
# Create account at https://ethereal.email
ETHEREAL_USER=
ETHEREAL_PASS=
